export const BASE_URL = String(import.meta.env.VITE_APP_BACKEND).trim();
export const REDUX_SECRET_KEY = String(import.meta.env.VITE_APP_SECRET_KEY).trim();
export const OCP_KEY = String(import.meta.env.VITE_APP_OCP_KEY).trim();

export const CLIENT_CODE = String(import.meta.env.VITE_APP_CLIENT_CODE).trim();

export const AUTH_TOKEN = String(import.meta.env.VITE_APP_AUTH_TOKEN).trim();

export const AUTH_TOKEN_USER = String(import.meta.env.VITE_APP_AUTH_TOKEN_USER).trim();
export const SOCKET_URL = String(import.meta.env.VITE_APP_SOCKET_URL).trim();
